---
source: https://developers.google.com/apps-script/reference/card-service/image-crop-type
root: apps-script
fetched_at: 2026-04-23T15:19:06.904Z
---

# Enum ImageCropType

## Enum ImageCropType

An enum that represents the crop styles applied to image components.

If you want to apply a crop style to an `IconImage`, you can only use `SQUARE ` or `CIRCLE`.

To call an enum, you call its parent class, name, and property. For example, ` CardService.ImageCropType.SQUARE`.
